/**
 * Write function that takes array of numbers
 * and return the index of the largest number
 */

function getLargestNumber (arrayOfNumbers){
    return largestNumberIndex
}

getLargestNumber([1, 22, 100, 3, 1]) // expects 2

